{
  "name": "transparenter",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "verify:core": "esbuild scripts/verify-core.ts --bundle --format=esm --platform=node --outfile=node_modules/.cache/verify-core.mjs --log-level=error && node node_modules/.cache/verify-core.mjs"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-dialog": "^2.7.1",
    "@tauri-apps/plugin-fs": "^2.5.1",
    "@tauri-apps/plugin-opener": "^2",
    "fast-png": "^8.0.0",
    "fflate": "^0.8.3"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "esbuild": "^0.28.1",
    "typescript": "~6.0.3",
    "vite": "^8.1.3"
  },
  "allowScripts": {
    "fsevents@2.3.3": true,
    "esbuild@0.28.1": true
  }
}
